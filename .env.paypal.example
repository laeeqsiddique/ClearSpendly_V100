# PayPal Integration Environment Variables
# Copy these to your .env.local file and fill in your actual values

# PayPal API Configuration
# Get these from your PayPal Developer Dashboard (https://developer.paypal.com)
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here

# PayPal Business Email (optional, used for payee configuration)
PAYPAL_BUSINESS_EMAIL=your_business_email@example.com

# Application URL (required for webhook URLs and payment redirects)
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# PayPal Environment (automatically determined based on NODE_ENV)
# sandbox = development/testing
# live = production
# This is set automatically - no need to configure manually

# PayPal Webhook Configuration
# These are configured automatically when setting up PayPal for a tenant
# Webhook URL format: ${NEXT_PUBLIC_APP_URL}/api/webhooks/paypal?tenant=${tenant_id}
# Required webhook events:
# - PAYMENT.CAPTURE.COMPLETED
# - PAYMENT.CAPTURE.DENIED
# - PAYMENT.CAPTURE.DECLINED
# - CHECKOUT.ORDER.APPROVED
# - PAYMENT.AUTHORIZATION.CREATED

# Notes:
# 1. For development, use PayPal sandbox credentials
# 2. For production, use live PayPal credentials
# 3. Ensure NEXT_PUBLIC_APP_URL is accessible from PayPal servers for webhooks
# 4. Each tenant will need their own PayPal Client ID for their business